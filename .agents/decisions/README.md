# Architecture decisions

ADRs record why a durable format, topology, persistence, security, dependency, marketplace, or release contract exists.

Read only decisions named by the selected domain/knowledge file or discovered while tracing the boundary. Do not load the directory by default.

Copy `0000-template.md`, assign the next four-digit number, and use a short kebab-case title. Accepted decisions are append-only; supersede them with a new ADR.