# Tactile Copilot adapter

The canonical vendor-neutral instructions are in `/AGENTS.md` and `.agents/`.

1. Read `/AGENTS.md`.
2. Route with `.agents/routing.md`.
3. Load only the selected workflow/domain and nearest local `AGENTS.md`.

Do not treat this adapter as a second source of project policy.
